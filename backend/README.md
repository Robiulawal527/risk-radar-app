# Risk Radar Backend

```bash
npm install
cp .env.example .env
npm run dev
```

API runs on `http://localhost:5000`. It loads Bangladesh crime stats from `data/crime-stats-2020-25.csv` and exposes dynamic heatmap, risk score, area details, reports, SOS, rankings, and alerts.
